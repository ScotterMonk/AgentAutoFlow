# Skill-local agent guidance: github-use

## Project Git environment
- Shell: Windows PowerShell in VS Code.
- If the virtual environment is deactivated and required for a verification command, reactivate with `./activate` before and separate from other commands.
- If GitHub credentials are requested, follow the credential guidance in the project-root `AGENTS.md` or relevant local rules file.

