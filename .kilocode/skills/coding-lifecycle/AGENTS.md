# Skill-local agent guidance: coding-lifecycle

## Project finish checks

- If VS Code exposes the Problems panel, inspect it before final completion and resolve relevant errors.
- For this repository, also prefer running focused `pytest` checks when code changed.
