# Skill-local agent guidance: coding-html

## Project editor guidance
- When editing Jinja templates in VS Code, use `jinja-html` language mode if available.
- This repository currently has no primary Jinja template tree; confirm template paths before assuming a web layout exists.

