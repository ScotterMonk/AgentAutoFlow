# Skill-local agent guidance: coding-debug

## Project debug backup location
- Save backups under `.kilocode/docs/old_versions/` with timestamped filenames before modifying files during debugging.
- Create the folder if it does not exist.

